import { roles } from './roles';

/**
 * DEPRECATED alias – use `roles` instead. Kept only to avoid massive refactor breakage.
 */
// eslint-disable-next-line @typescript-eslint/naming-convention
export const userGroups = roles;
