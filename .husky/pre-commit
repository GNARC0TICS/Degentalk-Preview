#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

# TEMPORARY DISABLE (2025-07-01): Prettier formatting step commented out to speed up development.
# To re-enable automatic formatting on commit, simply uncomment the line below.
# npm run format

# Ensure docs front-matter & lint pass
npm run docs:frontmatter
npm run docs:lint || exit 1

git add -A 