{
	"name": "degen-server",
	"version": "1.0.0",
	"type": "module",
	"scripts": {
		"dev": "tsx --tsconfig tsconfig.json --require tsconfig-paths/register index.ts",
		"build": "tsx compile . --outDir dist --tsconfig tsconfig.json",
		"start": "node dist/index.js",
		"lint": "eslint . --ext .ts,.tsx",
		"test": "jest",
		"lint:types": "tsc --noEmit"
	},
	"dependencies": {}
}
