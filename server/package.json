{
	"name": "degen-server",
	"version": "1.0.0",
	"type": "module",
	"scripts": {
		"dev": "tsx --tsconfig tsconfig.json --require tsconfig-paths/register index.ts",
		"build": "echo \"skip build (tsx runtime)\"",
		"start": "tsx --tsconfig tsconfig.json --require tsconfig-paths/register index.ts",
		"lint": "eslint . --ext .ts,.tsx",
		"test": "jest",
		"lint:types": "tsc --noEmit"
	},
	"dependencies": {
		"@supabase/supabase-js": "^2.50.0",
		"uuid": "^11.1.0"
	},
	"devDependencies": {
		"@types/uuid": "^10.0.0"
	}
}
