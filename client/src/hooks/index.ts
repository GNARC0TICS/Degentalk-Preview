// Auth hooks
export { useAuth } from './use-auth-static';

// UI hooks
export { useToast } from './use-toast';

// Responsive hooks - all from the canonical useMediaQuery file
export { useMediaQuery, useBreakpoint, useMobileDetector } from './useMediaQuery';
